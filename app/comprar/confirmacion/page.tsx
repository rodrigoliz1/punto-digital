import type { Metadata } from "next";
import Link from "next/link";
import { CheckCircle2 } from "lucide-react";
export const metadata: Metadata = { title: "Solicitud confirmada", robots: { index: false, follow: false } };
export default async function ConfirmationPage({ searchParams }: { searchParams: Promise<{ modo?: string; session_id?: string }> }) { const { modo, session_id: sessionId } = await searchParams; const demo = modo === "demo" || !sessionId; return <main className="state-page state-page--success"><CheckCircle2 size={50} /><span>{demo ? "Demostración" : "Pago enviado"}</span><h1>{demo ? "El recorrido de compra está listo para configurarse." : "Tu pago está siendo confirmado."}</h1><p>{demo ? "No se realizó ningún cargo ni se creó un proyecto. Para habilitar pagos reales deben configurarse Stripe, el webhook y la persistencia del pedido." : "Stripe notificará el resultado al servidor. Punto Digital confirmará tu pedido cuando el webhook haya sido procesado correctamente."}</p><div><Link className="button" href="/cotizador">Volver al cotizador</Link><Link className="button button--outline" href="/">Volver al inicio</Link></div></main>; }
